---
description: 将刚才的错误或用户偏好写入 AGENTS.md，避免下次重蹈覆辙
---

回顾对话，找出用户纠正了什么行为或表达了什么偏好，将其写入合适的 AGENTS.md。

如果 `$ARGUMENTS` 有内容，以用户传入的描述为准。

写入位置固定为当前项目根目录的 `AGENTS.md`；如果不存在就新建。不要写入 `~/.config/codewiz/AGENTS.md`。只追加，不修改已有内容。写入内容要简洁，能一句话说清楚就一句。写完后告知用户写了什么、写到哪里。

$ARGUMENTS
